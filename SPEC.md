# 找矿系统（Prospecting OS）产品规格

## 一句话

给 OPC（单人公司）用的"机会探矿"系统：把"找矿标志 → 信号分级 → 深挖证据 → 浅钻验证 → 建档回测"
的完整闭环做成可交互应用。数据持久化，单用户使用。

## 三个模块（完整闭环）

### 模块一：信号看板

- 六大找矿标志（作为信号分类）：
  1. 钱的流向（融资、并购、招标、预付款）
  2. 痛点的浓度（投诉、吐槽、用户自搭的土办法）
  3. 供给断层（需求起来、供给没跟上）
  4. 成本拐点（技术够用、价格打下来）
  5. 人才流向（聪明人用脚投票，如核心团队出走创业）
  6. 开源热度（GitHub star 增速等）
- 信号字段：标题、标志类型、描述、证据、来源链接、发现日期
- 三维度评分（每项 1–5 星）：证据强度、离钱近度、可切入度
- 综合分加权（证据 40% / 离钱 35% / 可切入 25%）自动分级：
  ≥4 分 = 富矿信号，2.5–4 分 = 观察中，<2.5 分 = 待排除；允许手动覆盖状态
- 列表支持按类型筛选、按综合分排序；每条信号可一键"转为浅钻"

### 模块二：深挖与浅钻验证

**深挖（研究动作，不触发验证）**：信号剖面里每条原始证据旁设独立入口，
可把证据改写成任意具体切口，指定想查清的内容和范围；研究代理定向寻找更多
案例、数字、主体、来源与反方证据，结果与日志挂回原信号。

**浅钻（验证动作，低成本探针）**：
- 探针字段：关联信号、探针类型（预售落地页 / 付费意愿访谈 / MVP / 原型验证 / 其他）、
  核心假设、预算成本、周期、状态（计划中 / 进行中 / 出矿了 / 没出矿）、结果记录、结论
- 浅钻原则：低成本、快验证，没出金就换地方
- 探针结论可一键"归档为矿区"（出矿 → 开采中/观察中；没出矿 → 坟场并写放弃原因）

深挖和浅钻是两个阶段，不绑定：先挖透，再决定要不要花钱验证。

### 模块三：矿区档案

- 档案字段：矿区名称、状态（开采中 / 观察中 / 已放弃·坟场）、信号回顾、探针结论、建档日期、回测日期
- 到期回测高亮提醒（回测日期已过且未标记完成）
- 坟场分区：已放弃的机会 + 放弃原因，防止重复挖坑

## 全局功能

- 首页统计条：信号总数、进行中探针数、待回测数
- 数据导出 / 导入 JSON（备份与迁移）
- 预置 3 条标注为"示例"的信号，方便上手即理解：
  1. 垂直行业 AI 获客 Agent（供给断层＋痛点浓度，评分示例偏高）
  2. 传统企业 AI 落地集成（供给断层，观察中）
  3. 新兴技术工具链（成本拐点，观察中偏早期）
- 原则库区：展示已沉淀的顶层原则；信号打分、探针设计引用原则自动执行
- 思维模型区：可检索的模型格栅，按易用性分级、按行业打标签；
  分析信号与设计探针时优先推荐"高易用性且行业对口"的模型
- 组织模式配置：OPC / 公司 / 集团（可扩展），资源适配度进评分、可一票否决

## 设计要求

- 全中文界面；深色主题＋金色点缀（探矿主题），专业克制不花哨
- 移动端可用
- 交互：Tab 切换模块，弹窗新增/编辑，操作后即时反馈
- 研究代理任务过程对用户可见：每条日志带时间戳展示，可追溯、可重试
