# 领域 Agent 框架

从找矿系统（Prospecting OS）实战中提炼的一套**有立场的简易 Agent 架构**。

## 洞察

为机会探矿造的不是一把铲子，而是一台"领域挖矿机"：

- **Workers**：研究代理、深挖代理——出去找证据、干活，不做最终决策
- **Hermes（路由/信使层）**：扫描 → 信号 → 深挖 → 浅钻 → 档案 pipeline，任务与结果在各阶段之间流转
- **原则库**：system prompt / constitution，给 agent 立规矩（只认有成本的证据、付钱是唯一标准……），已认同的常识沉淀进机器，不用每次口头重复
- **思维模型格栅**：推理工具箱，干活时按触发场景调用，而非常驻说教
- **矿脉分**：eval，对信号做证据强度 / 离钱近度 / 可切入度三维评分
- **矿区档案 + 定期回测**：verifier + trace，决策留档，从错误里提炼原则

## 与通用框架的区别

通用框架（如 LangChain 类）无立场，什么都能搭，所以什么都得从头搭；
这套架构有立场——原则库为特定领域调教。好用的原因正是它只能干这一件事。

代价是明确的：换领域 = 重写原则库 + 重定义信号。这不是缺陷，是设计。

## 复用公式

```
新领域 = 换一套原则库 + 换一套信号定义 + 复用架构（agents + 路由 + eval + 档案回测）
```

找矿系统是第一个实例：原则库 = OPC 找矿原则，信号 = 六大找矿标志。

## 组织模式：可配置的资源约束

"资源适配度"应该是可配置维度，而非写死：

- **OPC 模式**（默认）：资源 = 1 人 + 1 AI。信息处理产出约等于 2–3 个普通人，
  但信任、关系、线下履约、牌照资质环节 AI 补不上。通过标准："AI 能吃掉该方向 70% 工作量"。
- **公司模式 / 集团模式**（可扩展）：不同组织形态，可承载的机会集合完全不同。

信号进入评分前，先过"组织模式"这道筛：吃不下的机会，分再高也不碰。

## 为什么这可能是更大的矿

- 痛点通用：每个想单干的人都需要自己的"领域机器"，但没人会从头搭
- 框架本身可产品化：垂直 Agent 搭建器 / 领域机器模板
- 拼图完整：决策机器（管"做不做"）+ 机会机器（管"找什么"）+ 框架 = 个人公司操作系统

## 验证标准

先把第一个实例跑顺，用实战验证架构本身；等真正赚到钱了，才算认可这个架构。
在此之前，框架只是假设——

**机器用"有没有人付钱"验证机会，人用"有没有赚到钱"验证机器。**
